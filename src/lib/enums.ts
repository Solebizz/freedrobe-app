export enum Status {
	open = 'open',
	closed = 'closed',
}

export enum AttendanceStatus {
	present = 'present',
	absent = 'absent',
}

export enum MembershipTypeEntry {
	expired = 'expired',
	start = 'start',
	upgrade = 'upgrade',
}

export enum OrderStatus {
	paid = 'paid',
	cancelled = 'cancelled',
	refund = 'refund',
	pending = 'pending',
}

export enum PlayerRank {
	prime = 'prime',
	elite = 'elite',
	developing = 'developing',
}
